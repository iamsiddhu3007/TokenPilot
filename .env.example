# === MANDATORY (the FOUR platforms) ===
ROCKETRIDE_API_KEY=
ROCKETRIDE_ENDPOINT=

BUTTERBASE_API_KEY=
BUTTERBASE_PROJECT_URL=
BUTTERBASE_GATEWAY_KEY=

XTRACE_API_KEY=
XTRACE_ENDPOINT=

# Photon / Spectrum (sign up at app.photon.codes → project id + secret)
PHOTON_PROJECT_ID=
PHOTON_SECRET=
PHOTON_TARGET=          # slack channel / phone / handle to push notifications to

# === DATA ===
DATASET_PATH=data/seed_tickets.json

# === OPTIONAL (direct LLM fallback — insurance only) ===
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# === SERVER ===
PORT=3001
